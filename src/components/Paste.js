function Paste({ title, author, date, category, body }) {
    return (
        <div>
            <p>Title: {title}</p>
            <p>Author: {author}</p>
        </div>

    )
}
export default Paste;